let cart=[];function add(name,price){cart.push({name,price});render()}function removeItem(i){cart.splice(i,1);render()}function render(){document.getElementById('count').textContent=cart.length;document.getElementById('items').innerHTML=cart.length?cart.map((p,i)=>`<div style="display:flex;justify-content:space-between;margin:8px 0">${p.name} - $${p.price.toFixed(2)} <button onclick="removeItem(${i})">×</button></div>`).join(''):'No has agregado productos.';document.getElementById('total').textContent=cart.reduce((s,p)=>s+p.price,0).toFixed(2)}function checkout(){if(!cart.length)return alert('Agrega un producto primero.');alert('PayPal todavía no está conectado. Configura un sistema de pago seguro antes de publicar.')}render();